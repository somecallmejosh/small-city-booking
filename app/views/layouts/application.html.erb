<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Small City Studio" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Small City">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <meta name="vapid-public-key" content="<%= Rails.application.config.vapid_public_key %>">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-stone-50 font-sans antialiased">
    <nav class="bg-stone-900 text-white">
      <div class="mx-auto flex max-w-6xl items-center justify-between px-4 py-3 lg:px-8">
        <%= link_to "Small City Studio", root_path, class: "text-sm font-semibold tracking-wide hover:text-stone-300" %>
        <div class="flex items-center gap-6 text-sm">
          <%= link_to "Help", help_path, class: "hover:text-stone-300 #{'text-stone-300' if current_page?(help_path)}" %>
          <% if authenticated? %>
            <%= link_to "My Bookings", bookings_path, class: "hover:text-stone-300" %>
            <div data-controller="push-subscription"
                 data-push-subscription-vapid-public-key-value="<%= Rails.application.config.vapid_public_key %>">
              <button data-push-subscription-target="button"
                      data-action="push-subscription#toggle"
                      class="text-stone-400 hover:text-white text-sm hidden">
                Notifications
              </button>
            </div>
            <%= button_to "Sign Out", session_path, method: :delete,
                  class: "text-stone-400 hover:text-white text-sm" %>
          <% else %>
            <%= link_to "Sign In", new_session_path, class: "hover:text-stone-300" %>
          <% end %>
        </div>
      </div>
    </nav>

    <% flash.each do |type, message| %>
      <div class="mx-auto max-w-6xl px-4 pt-4 lg:px-8">
        <%= render FlashMessageComponent.new(type: type.to_sym, message: message) %>
      </div>
    <% end %>

    <main class="mx-auto max-w-6xl px-4 py-8 lg:px-8">
      <%= yield %>
    </main>
  </body>
</html>
