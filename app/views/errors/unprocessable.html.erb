<% content_for :title, "Request Error — Small City Studio" %>

<div class="flex flex-col items-center justify-center py-24 text-center">
  <p class="text-8xl font-bold text-stone-200 select-none">422</p>
  <h1 class="mt-4 text-2xl font-semibold text-stone-900">Request could not be processed</h1>
  <p class="mt-2 max-w-sm text-stone-500">
    Something went wrong with your request. This can happen if your session expired or the page was submitted twice.
  </p>
  <%= link_to "Back to home", root_path, class: btn_primary + " mt-6" %>
</div>
